# Override the backend WebSocket URL if it's not running on localhost:8000.
# Copy this file to .env.local and edit as needed — Vite loads .env.local
# automatically and it's gitignored, so it's safe to put local URLs there.
VITE_WS_URL=ws://localhost:8000/ws
